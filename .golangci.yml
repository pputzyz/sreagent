version: "2"

run:
  timeout: 3m

linters:
  default: standard
  disable:
    - exhaustruct
    - depguard
    - ireturn
    - varnamelen
    - wsl
    - nlreturn
    - godot
    - testpackage

formatters:
  enable:
    - gofmt
